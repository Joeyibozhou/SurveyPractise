package com.Survey.SchedulingSurvey.service;

/*import com.Survey.SchedulingSurvey.entity.Survey;
import com.Survey.SchedulingSurvey.repository.SurveyRepository;
import org.springframework.beans.factory.annotation.Autowired;

public class SurveyService {

    @Autowired
    private SurveyRepository repository;

    public Survey saveSurvey(Survey survey) {
        String tmp_email= survey.getEmail();
        Survey oldSurvey = repository.findByEmail(tmp_email);

            return repository.save(survey);

    }
}*/
